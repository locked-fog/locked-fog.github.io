---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout pageTitle="关于">
  <section class="content-stack">
    <article class="panel panel--classified-3 panel--bracket-tr panel--bracket-bl prose-panel">
      <span class="micro-label micro-label--accent">WRITER MODE</span>
      <h2>你的实际工作只需要两件事</h2>
      <div class="prose">
        <ol>
          <li>在 <code>src/content/posts/</code> 里新建一个 Markdown 文件。</li>
          <li>写 frontmatter、正文和公式，然后提交到 GitHub。</li>
        </ol>

        <p>不需要手动把 Markdown 转 HTML，也不需要提前把 LaTeX 公式截图或导出图片。</p>

        <h3>推荐写作语法</h3>
        <ul>
          <li>行内公式：<code>$E = mc^2$</code></li>
          <li>块级公式：<code>$$ ... $$</code></li>
          <li>代码块：使用三个反引号包裹</li>
          <li>脚注：使用 GitHub Flavored Markdown 的 <code>[^1]</code> 语法</li>
        </ul>

        <h3>文章模板位置</h3>
        <p><code>src/content/posts/_template.md</code> 里已经放好了可复制的模板。</p>
      </div>
    </article>

    <article class="panel panel--classified-4 prose-panel">
      <span class="micro-label">PUBLISHING</span>
      <h2>第一次发布时的最简单选择</h2>
      <div class="prose">
        <p>最省事的做法是把仓库名直接建成 <code>&lt;你的 GitHub 用户名&gt;.github.io</code>。</p>
        <p>这样部署地址就是站点根目录，不需要额外配置 Astro 的 <code>base</code>。</p>
        <p>如果以后你再换成自定义域名，只需要在 <code>public/CNAME</code> 中加入域名，并到 GitHub Pages 设置里绑定即可。</p>
      </div>
    </article>
  </section>
</BaseLayout>
